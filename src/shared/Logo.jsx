export function Logo({ onClick }) {
  return (
    <button
      onClick={onClick}
      className="text-lg tracking-tight font-semibold text-slate-700 dark:text-slate-300 hover:text-accent hover:font-bold dark:hover:text-accent"
    >
      Anfisa Domashova
    </button>
  );
}
